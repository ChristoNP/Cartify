/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'www.themgoods.com', 'www.fmicassets.com', 'nafiriguitar.com', 'digitalassets.tesla.com', 'loremflickr.com', 'media.giphy.com'],
    },
  }

export default nextConfig;
